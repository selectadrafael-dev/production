<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- HIDE DISCUSS -->
    <record id="mail.menu_root_discuss"
            model="ir.ui.menu">

        <field name="groups_id"
               eval="[(3, ref('gift_product_configurator.group_product_vendor'))]"/>

    </record>

    <!-- HIDE CONTACTS -->
    <record id="contacts.menu_contacts"
            model="ir.ui.menu">

        <field name="groups_id"
               eval="[(3, ref('gift_product_configurator.group_product_vendor'))]"/>

    </record>

    <!-- HIDE APPS -->
    <record id="base.menu_administration"
            model="ir.ui.menu">

        <field name="groups_id"
               eval="[(3, ref('gift_product_configurator.group_product_vendor'))]"/>

    </record>

</odoo>