<odoo>
  <data>

    <record id="product_public_category_form_hero"
            model="ir.ui.view">

      <field name="name">product.public.category.hero.form</field>
      <field name="model">product.public.category</field>

      <!-- Parent view -->
      <field name="inherit_id"
             ref="website_sale.product_public_category_form_view"/>

      <field name="arch" type="xml">

        <!-- Insert inside main sheet -->
        <xpath expr="//sheet" position="inside">

          <group string="Hero Banner" colspan="2">

            <!-- 🔹 Layout Selection -->
            <group>
              <field name="hero_layout"/>
              <field name="hero_bg_color" widget="color"/>
            </group>

            <!-- 🔹 Content -->
            <group>
              <field name="hero_title"/>
              <field name="hero_subtitle"/>
              <field name="hero_description"/>
               <field name="show_favourites"/>
              <field name="show_home_promo"/>
              <field name="show_popular_carousel"/>
            </group>

          </group>

        </xpath>

      </field>

    </record>

  </data>
</odoo>