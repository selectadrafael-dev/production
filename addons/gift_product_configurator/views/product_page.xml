<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="gift_custom_product_page" inherit_id="website_sale.product">
        
        <!-- HIDE original columns to prevent stacking -->
        <xpath expr="//div[hasclass('col-lg-7')]" position="attributes">
            <attribute name="class" add="d-none" separator=" " />
        </xpath>
        <xpath expr="//div[@id='product_details']" position="attributes">
            <attribute name="class" add="d-none" separator=" " />
        </xpath>

        <!-- INJECT Custom Layout -->
        <xpath expr="//section[@id='product_detail']" position="inside">
            
            <!-- TOP ORANGE BANNER -->
            <div class="gift-top-banner text-center py-2 mb-4" style="background-color: #FF8C00; color: white;">
                <div class="fw-bold">⭐ CLICK HERE FOR OUR MOST POPULAR PICKS ⭐</div>
                <div class="small">Buy tried and tested items customers love</div>
            </div>

            <div class="container-fluid px-lg-5 gift-main-body">
                <div class="row g-4 align-items-start">
                    
                    <!-- COLUMN 1: STICKY GALLERY -->
                    <div class="col-lg-5 col-12 sticky-gallery">
                        <div class="border rounded p-3 bg-white position-relative shadow-sm text-center">
                            <span class="badge rounded-pill bg-danger position-absolute top-0 end-0 m-3 p-3 z-index-1" style="font-size: 1.2rem;">15% OFF</span>
                            <!-- JS will move Odoo's carousel here -->
                            <div id="gift_gallery_hook"></div>
                        </div>
                    </div>

                    <!-- COLUMN 2: CONFIGURATOR (SCROLLING) -->
                    <div class="col-lg-4 col-12">
                        <h1 t-field="product.name" class="h2 fw-bold mb-1" />
                        <div class="text-muted mb-4 small">Product Code: <span t-field="product.default_code" /></div>
                        
                        <!-- JS will move Odoo's variant buttons here -->
                        <div id="gift_variant_hook" class="mb-5"></div>

                        <!-- QUANTITY GRID -->
                        <div class="gift-qty-section mt-5 border-top pt-4">
                            <div class="row g-2">
                                <t t-foreach="[50, 100, 250, 500, 1000, 2500, 5000, 10000]" t-as="qty">
                                    <div class="col-6 col-md-3">
                                        <div t-attf-class="tier-card p-2 border rounded text-center #{'border-primary active bg-light' if qty == 50 else ''}">
                                            <div class="fw-bold"><t t-esc="qty" />+</div>
                                            <div class="small">£<t t-esc="'%.2f' % (product.list_price * 0.8)" /></div>
                                            <div class="x-small text-muted">each</div>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>

                    <!-- COLUMN 3: QUOTE PANEL (STICKY) -->
                    <div class="col-lg-3 col-12 sticky-sidebar">
                        <div class="border rounded p-4 bg-white shadow-sm text-center">
                            <div class="h2 fw-bold mb-0 text-primary">
                                <t t-call="website_sale.product_price" />
                            </div>
                            <div class="small text-muted mb-4">per unit (exc. VAT)</div>
                            
                            <button class="btn btn-primary w-100 py-3 fw-bold mb-2 shadow-sm" style="background-color: #002D5B; border: none;">Book A Call Back</button>
                            
                            <div class="trustpilot-box mt-4 pt-3 border-top text-start">
                                <div class="small text-muted mb-2">Showing our 5 star reviews</div>
                                <div class="testimonial border-start border-3 border-success ps-2 mb-3">
                                    <div class="fst-italic small">"Excellent service from start to finish..."</div>
                                    <div class="fw-bold x-small mt-1">James Dickinson, 2 days ago</div>
                                </div>
                                <img src="https://upload.wikimedia.org" style="height: 18px; opacity: 0.6;" />
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </xpath>
    </template>
</odoo>
