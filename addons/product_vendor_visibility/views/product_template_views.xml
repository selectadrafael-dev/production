<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Product Search View -->
    <record id="product_template_search_vendor"
            model="ir.ui.view">

        <field name="name">
            product.template.search.vendor
        </field>

        <field name="model">
            product.template
        </field>

        <field name="inherit_id"
               ref="product.product_template_search_view"/>

        <field name="arch"
               type="xml">

            <!-- SEARCH -->
            <xpath expr="//search"
                   position="inside">

                <field name="vendor_id"/>

                <field name="vendor_fingerprint"/>

            </xpath>

            <!-- GROUP BY -->
            <xpath expr="//group"
                   position="inside">

                <filter
                    string="Vendor"
                    name="group_by_vendor"
                    context="{'group_by':'vendor_id'}"/>

            </xpath>

        </field>

    </record>

</odoo>